# Python 3.9+ required
# Unpinned for cloud compatibility — pip will resolve correct versions for Python 3.12.
# Heavy ML (torch/transformers/sentence-transformers) intentionally omitted.
# model_manager.py handles graceful fallback to heuristics when ML is absent.
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.25.0
scipy>=1.11.0
networkx>=3.1
matplotlib>=3.7.0
plotly>=5.17.0
scikit-learn>=1.3.0
regex>=2023.6.3
pydantic>=2.0.0
PyPDF2>=3.0.0
python-dotenv>=1.0.0
google-generativeai>=0.8.0

vaderSentiment==3.3.2
openai>=1.0.0
groq>=1.0.0
